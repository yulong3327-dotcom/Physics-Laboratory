# 分镜生成耗时检查（2026-09-21）

检查项目：`video-218a0769-6466-42d7-9375-f2fd9d1c0cc6`。读取任务快照、请求指标及实现代码；未修改生产逻辑，未请求模型，未停止或恢复任务。时间均为北京时间。

## 结论

本次首张迟迟不出现的直接原因是模型请求等待、95 秒超时后拆批重来，以及首批完整 JSON 验收前无法发布任何画面。任务尚未进入画面准备，不能将这次等待归因为本地渲染性能。相同输入已有完整结果，本次因登录会话参与去重又从头生成，是额外的可避免耗时。

## 本次时间线

任务：`task-6c1afeec-c3f9-4331-b976-333cd13b3ba4`，输入第 3 版，29 段台词、约 1050 字，无剧情分类，走知识分镜管线。

| 北京时间 | 动作 | 耗时与结果 |
| --- | --- | --- |
| 10:54:57.155 | 创建任务 | 几乎立即请求模型，无分钟级排队 |
| 10:54:57.189–10:55:47.835 | 全课结构 | 50.646 秒，成功；1662 输入 tokens、1581 输出 tokens |
| 10:55:47.891–10:57:22.900 | 首批 u1–u6 分镜 | 95.009 秒超时；请求约 19.3 KB，没有可用模型输出 |
| 10:57:22 后 | 自动拆为 u1–u3 与 u4–u6，先请求前三段 | 继续等待约 42 秒，取消前没有成功响应 |
| 10:58:05.041 | 任务变为 cancelled | 总计约 187.9 秒；已完成 0 张 |

第三次请求已经发起，但取消路径没有保存其最终耗时，所以约 42 秒来自时间线推算，不是完整请求指标。以上取消状态来自任务记录，本次检查未执行取消。

## 历史样本

任务 `task-af4c875b-f229-464a-9e89-6fe6dea63527` 已完成 23 张、覆盖 29/29 段，尚未采用。

- 8 次有记录的成功分镜调用用时 43.290–94.612 秒，平均 66.628 秒；实际输入约 5474–5819 tokens、输出 1274–3318 tokens。
- 2 次请求各约 95 秒超时，合计损耗 190.028 秒；另记录 1 次内容修正。
- 11 条已落盘请求记录合计 756.194 秒，即 12 分 36 秒；请求计数为 12，因此这不是完整总耗时。
- 最后一次“电路画面准备”为 29 毫秒，只代表最后准备阶段，不包含所有逐张预览、Python 场景渲染或成片制作。
- 历史分镜阶段计时小于该阶段请求记录之和，说明重启/中断导致阶段统计不完整。不能用两者相减估算本地处理耗时。
- 页面“已用时”按 createdAt 到 now/updatedAt 计算，会包含暂停和后续元数据更新，不能当作有效生成耗时。

## 原因与代码证据

1. 相同输入被重新生成。两个任务的 input.json 深比较完全相同，projectId、revision、kind、scope、instruction 相同，credentialProfileId 不同，最终 inputHash 不同。`server/videoTasks.ts:313` 将 credentialProfileId 纳入去重；`server/aiSession.ts:74` 每次登录产生新 UUID。该行为不证明两次使用了相同模型，但证明结果复用被短期登录会话绑定。当前任务指标未保存实际模型/服务地址，无法进一步区分上游配置变化。
2. 首张展示受整批响应约束。`server/videoGeneration.ts:202` 先请求全课结构；`:229` 开始批次。`src/lib/videoStoryboardBatch.ts:283` 等待完整候选、验收后在 `:363` 提交；`server/videoGenerationPreview.ts` 才逐张准备与发布。逐张展示不等于模型逐镜头生成。
3. 批次默认 6 段/1000 字，单批复杂输出。`server/videoGeneration.ts:231`；`src/lib/videoProject.ts:258` 同时要求镜头、板书、公式、同步、高亮、电路等。知识主提示词约 6740 字符/12781 字节，再附加上下文。6 段输出上限 10800 tokens，不代表一定用满。
4. 超时后付出重做成本。`server/videoGeneration.ts:51` 默认 95 秒截止；`src/lib/videoStoryboardBatch.ts:329` 首次超时二分。本次已经实际发生。历史成功请求有 94.6 秒样本，截止值距离成功耗时上沿很近；仅增加超时也会扩大异常等待，不能单独作为完整优化方案。
5. 串行与整批纠错。`src/lib/videoStoryboardBatch.ts:256` 串行请求；`:341` 内容失败最多进行初次请求加两次修正。共享电路、前文和跨批公式引用形成依赖，不宜直接对所有批次盲目并发。
6. 次要开销待测。逐张预览反复处理增长前缀，部分电路变化可能使已准备后缀重建；最后完整准备未使用预览 baseline。这些代码存在额外工作，但本次 0 张完成，不能作为本次首张延迟主因。进度轮询 2 秒也不能解释分钟级等待。

## 建议处理顺序

1. 先复用已有 23 张候选结果，在后台任务中查看差异并采用；重新生成应是明确操作。重设去重为项目内容、生成配置、提示词版本等稳定身份，保持权限隔离；登录会话只用于本次请求授权。
2. 首批采用更小工作量，保留全课上下文；后续按实测耗时和复杂度调整。一次返回可独立校验的一张/少量镜头，成功即发布，不解析尚未闭合的 JSON 来冒充完成。首批缩小与总吞吐需要分别测量。
3. 将共享电路规划与逐镜头编排拆开，减少重复拓扑输出；局部字段错误尽量确定性修正或局部请求，保留现有知识教学规则。
4. 仅对明确无依赖的镜头采用有限并发，继续保留逐张落盘、取消隔离、断点与顺序拼接。
5. 补齐首字节/完整响应、超时/取消、校验、几何、写盘、预览及有效运行时间等指标，区分模型/网关等待与本地执行。现有日志不能拆分服务商排队、网络与模型计算，也不能确定实际生效模型。

以上为检查结果与改进顺序，本次未实施性能改动。
